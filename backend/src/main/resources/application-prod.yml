springdoc:
  api-docs:
    enabled: false

  swagger-ui:
    enabled: false

app:
  jwt:
   secret: ${JWT_SECRET}
  storage:
    type: s3

logging:
  level:
    root: INFO
    com.bankflow.filter.JwtAuthenticationFilter: INFO